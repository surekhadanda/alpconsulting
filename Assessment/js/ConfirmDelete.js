// JScript File

function confirm_delete()
{
  if (confirm("Are you sure you want to delete the Record")==true)
    return true;
  else
    return false;
}
function confirm_UnArchive()
{
  if (confirm("Are you sure you want to Restore the Documents")==true)
    return true;
  else
    return false;
}